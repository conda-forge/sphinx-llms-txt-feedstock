{% set name = "sphinx-llms-txt" %}
{% set version = "0.5.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/sphinx_llms_txt-{{ version }}.tar.gz
  sha256: 118322810f3e1ff4076d8a5657b360747e30bd0cdafff91987e7a17247353685

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - pip
  run:
    - python >={{ python_min }}

test:
  imports:
    - sphinx_llms_txt
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}
    - sphinx

about:
  home: https://github.com/jdillard/sphinx-llms-txt
  summary: llms-full.txt generator for Sphinx
  license: MIT
  license_file: LICENSE
  dev_url: https://github.com/jdillard/sphinx-llms-txt
  doc_url: https://sphinx-llms-txt.readthedocs.io

extra:
  recipe-maintainers:
    - jdillard
